components:
  schemas:
    loginInformation:
      type: object
      properties:
        user_id:
          type: string
        token:
          type: string
    loginRequest:
      type: object
      properties:
        login:
          type: string
        password:
          type: string
    signupInfo:
      type: object
      properties:
        username:
          type: string
        password:
          type: string